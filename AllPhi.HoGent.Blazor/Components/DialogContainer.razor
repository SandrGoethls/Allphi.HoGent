@if (Visible)
{
    <div class="show-dialog-background">
        @ChildContent
    </div>
}

@code {
    [Parameter, EditorRequired] public RenderFragment ChildContent { get; set; }
    [Parameter] public bool Visible { get; set; }
}
